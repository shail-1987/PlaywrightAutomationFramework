export const ENV = {
    dev: {
        baseURL: "http://localhost:8080"
    },

    qa: {
        baseURL: "http://localhost:8080"
    },

    uat: {
        baseURL: "http://localhost:8080"
    },

    prod: {
        baseURL: "http://localhost:8080"
    }
};

export const CURRENT_ENV = ENV.dev;